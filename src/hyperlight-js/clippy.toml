disallowed-macros = [
    { path = "std::assert", reason = "no asserts in release library builds" },
    { path = "std::assert_eq", reason = "no asserts in release library builds" },
    { path = "std::assert_ne", reason = "no asserts in release library builds" },
]